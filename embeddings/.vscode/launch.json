{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Embeddings MCP Server",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/embeddings.py",
      "cwd": "${workspaceFolder}",
      "python": "${workspaceFolder}/.venv/bin/python",
      "console": "integratedTerminal"
    }
  ]
}
